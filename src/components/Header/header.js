import "../../index.css";

const Header = (props) => {
    return (
        <div>
            <header  className="header" >
               <h1>Fast React Pizza Co.</h1>
               
             </header>
        </div>
    );
}

export default Header;